# {项目名称} 技术方案

**版本**: v1.0
**日期**: {生成日期}
**作者**: {作者}
**状态**: 待评审

---

## 1. 背景和目标

### 1.1 背景
{描述当前问题或业务场景，说明为什么要进行这次改动}

### 1.2 目标
{描述本次改动要达到的具体目标}

**成功标准**:
- {可衡量的成功指标 1}
- {可衡量的成功指标 2}

---

## 2. 改动范围

### 2.1 包含内容
| 模块/功能 | 描述 | 优先级 |
|----------|------|--------|
| {模块A} | {功能描述} | P0 |
| {模块B} | {功能描述} | P1 |

### 2.2 不包含内容
| 内容 | 原因 |
|------|------|
| {功能X} | {不在本次范围的原因} |
| {功能Y} | {计划在后续版本实现} |

---

## 3. 现状梳理（可选）

### 3.1 当前架构
{描述当前系统架构和关键组件}

### 3.2 存在问题
| 问题 | 影响 | 优先级 |
|------|------|--------|
| {问题1} | {影响描述} | 高 |
| {问题2} | {影响描述} | 中 |

---

## 4. 总体方案

### 4.1 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    [架构层级/区域]                        │
│                                                          │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │ [组件A]  │───▶│ [组件B]  │───▶│ [组件C]  │         │
│  └──────────┘    └──────────┘    └──────────┘         │
│       │               │               │               │
│       ▼               ▼               ▼               │
│  ┌───────────────────────────────────────────┐       │
│  │            [共享组件/数据层]                │       │
│  └───────────────────────────────────────────┘       │
│                                                          │
└─────────────────────────────────────────────────────────┘

图例说明：
- ▶ 同步调用
- ⬆ 异步消息
- ─ 数据流向
```

### 4.2 时序图

```
用户    ──▶  API网关  ──▶  服务A  ──▶  服务B  ──▶  数据库
 │             │           │           │           │
 │  ①请求      │           │           │           │
 ├────────────▶│           │           │           │
 │             │  ②转发    │           │           │
 │             ├──────────▶│           │           │
 │             │           │  ③调用    │           │
 │             │           ├──────────▶│           │
 │             │           │           │  ④查询    │
 │             │           │           ├──────────▶│
 │             │           │           │  ⑤返回    │
 │             │           │           │◀──────────┤
 │             │           │  ⑥响应    │           │
 │             │           │◀──────────┤           │
 │             │  ⑦返回    │           │           │
 │◀────────────┤           │           │           │
 │  ⑧响应      │           │           │           │
```

### 4.3 核心流程说明
1. {步骤1描述}
2. {步骤2描述}
3. {步骤3描述}

---

## 5. 关键设计点

### 5.1 接口变更（可选）

#### HTTP 接口
**接口**: `POST /api/v1/{resource}`

**请求参数**:
```json
{
  "param1": "string",   // 参数说明
  "param2": 123,        // 参数说明
  "param3": {           // 嵌套对象
    "field1": "value"
  }
}
```

**响应结果**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "result": "具体数据"
  }
}
```

**兼容策略**:
- {描述新旧版本兼容方案}
- {灰度发布策略}
- {回滚方案}

#### RPC 接口（如适用）
**服务**: {ServiceName}
**方法**: {MethodName}

**请求参数**:
```protobuf
message Request {
  string field1 = 1;     // 字段说明
  int32 field2 = 2;      // 字段说明
}
```

**响应结果**:
```protobuf
message Response {
  string result = 1;     // 字段说明
  int32 code = 2;        // 状态码
}
```

---

### 5.2 数据模型/表结构变更（可选）

#### 新增表
**表名**: `{table_name}`

```sql
CREATE TABLE `{table_name}` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
  `field1` VARCHAR(255) NOT NULL COMMENT '字段1',
  `field2` INT DEFAULT 0 COMMENT '字段2',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_field1` (`field1`) COMMENT '字段1索引'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='{表说明}';
```

#### 修改表
**表名**: `{table_name}`

**DDL变更**:
```sql
-- 新增字段
ALTER TABLE `{table_name}` ADD COLUMN `new_field` VARCHAR(100) COMMENT '新字段';

-- 新增索引
ALTER TABLE `{table_name}` ADD INDEX `idx_new_field` (`new_field`);
```

#### 数据迁移
{描述数据迁移策略，包括：
- 数据迁移脚本
- 回滚方案
- 数据一致性验证}

---

### 5.3 异步/任务/幂等/重试（可选）

#### 异步任务
**任务类型**: {任务类型描述}

**任务配置**:
```yaml
task_name: {任务名称}
retry_times: 3           # 重试次数
timeout: 30s             # 超时时间
dead_letter: true        # 死信队列
```

**处理逻辑**:
1. {步骤1}
2. {步骤2}
3. {步骤3}

#### 幂等设计
| 场景 | 幂等方案 | 实现方式 |
|------|----------|----------|
| {场景1} | {方案描述} | {实现细节} |
| {场景2} | {方案描述} | {实现细节} |

**幂等键设计**:
```
idempotent_key = {业务前缀}_{用户ID}_{业务ID}_{时间戳}
```

#### 重试策略
| 错误类型 | 重试次数 | 重试间隔 | 退避策略 |
|----------|----------|----------|----------|
| 网络超时 | 3次 | 1s, 2s, 4s | 指数退避 |
| 服务繁忙 | 2次 | 5s, 10s | 固定间隔 |

---

### 5.4 权限/审计/安全（可选）

#### 权限设计
| 资源 | 操作 | 角色 | 权限控制点 |
|------|------|------|-----------|
| {资源A} | 读写 | {角色X} | API层 |
| {资源B} | 只读 | {角色Y} | 数据层 |

**权限模型**: {RBAC/ABAC/ACL}

#### 审计日志
**日志内容**:
- 操作人
- 操作时间
- 操作类型
- 操作资源
- 操作结果
- IP地址

**存储位置**: {日志存储方案}

#### 安全设计
| 安全点 | 方案 | 实施方式 |
|--------|------|----------|
| 数据加密 | {加密方案} | {实施细节} |
| 访问控制 | {控制方案} | {实施细节} |
| 防重放 | {防重放方案} | {实施细节} |

---

### 5.5 服务角色职责（可选）

| 服务/组件 | 职责 | 依赖 | 提供者 |
|----------|------|------|--------|
| {服务A} | {职责描述} | {依赖列表} | {团队} |
| {服务B} | {职责描述} | {依赖列表} | {团队} |

---

### 5.6 上下游依赖（可选）

#### 上游依赖
| 系统/服务 | 依赖内容 | SLA | 降级方案 |
|----------|----------|-----|----------|
| {上游A} | {依赖描述} | 99.9% | {降级策略} |

#### 下游影响
| 系统/服务 | 影响内容 | 兼容性 | 通知方式 |
|----------|----------|--------|----------|
| {下游A} | {影响描述} | 完全兼容 | {通知方式} |

---

### 5.7 其他设计
{根据具体场景添加其他设计要点}

---

## 6. 改动清单

### 6.1 按项目分组

| 项目 | 模块/包路径 | 文件 | 改动类型 | 说明 |
|------|-----------|------|----------|------|
| {项目A} | {package.path} | {File.java} | 新增/修改/删除 | {改动说明} |
| {项目A} | {package.path} | {File.ts} | 修改 | {改动说明} |
| {项目B} | {package.path} | {File.go} | 新增 | {改动说明} |

### 6.2 配置变更
| 环境 | 配置项 | 旧值 | 新值 | 影响范围 |
|------|--------|------|------|----------|
| 生产 | {config.key} | {old} | {new} | {影响} |

### 6.3 依赖变更
| 依赖 | 旧版本 | 新版本 | 变更原因 |
|------|--------|--------|----------|
| {lib} | 1.0.0 | 2.0.0 | {原因} |

---

## 7. 测试计划与结果

### 7.1 测试范围
| 测试类型 | 覆盖范围 | 负责人 |
|----------|----------|--------|
| 单元测试 | {覆盖范围} | {负责人} |
| 集成测试 | {覆盖范围} | {负责人} |
| 接口测试 | {覆盖范围} | {负责人} |
| 性能测试 | {覆盖范围} | {负责人} |

### 7.2 接口测试示例

#### 测试用例 1：{用例名称}
**接口**: `POST /api/v1/{resource}`

**请求示例**:
```json
{
  "param1": "test_value",
  "param2": 123
}
```

**预期响应**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 12345,
    "result": "expected_result"
  }
}
```

**验证点**:
- [ ] 响应码为 200
- [ ] code 字段为 0
- [ ] data.result 值正确

### 7.3 本地测试结果
| 测试项 | 结果 | 备注 |
|--------|------|------|
| 单元测试 | ✅ 通过 | 覆盖率 85% |
| 集成测试 | ✅ 通过 | 所有场景通过 |
| 接口测试 | ⏳ 待执行 | 预计本周完成 |

### 7.4 CI 测试结果
**CI Pipeline**: {pipeline_url}

**测试报告**: {report_url}

**结果**: ✅ 通过 / ❌ 失败

**失败项**:
- {失败项1}: {原因分析}
- {失败项2}: {原因分析}

---

## 8. 风险评估

### 8.1 技术风险
| 风险项 | 概率 | 影响 | 应对措施 | 责任人 |
|--------|------|------|----------|--------|
| {风险1} | 高/中/低 | 高/中/低 | {应对方案} | {负责人} |
| {风险2} | 高/中/低 | 高/中/低 | {应对方案} | {负责人} |

### 8.2 业务风险
| 风险项 | 概率 | 影响 | 应对措施 | 责任人 |
|--------|------|------|----------|--------|
| {风险1} | 高/中/低 | 高/中/低 | {应对方案} | {负责人} |

### 8.3 进度风险
| 风险项 | 概率 | 影响 | 应对措施 | 责任人 |
|--------|------|------|----------|--------|
| {风险1} | 高/中/低 | 高/中/低 | {应对方案} | {负责人} |

### 8.4 回滚方案
**触发条件**:
- {条件1}
- {条件2}

**回滚步骤**:
1. {步骤1}
2. {步骤2}
3. {步骤3}

**回滚验证**:
- {验证点1}
- {验证点2}

---

## 附录

### A. 参考资料
- {文档链接/描述}
- {相关技术文档}
- {类似方案参考}

### B. 评审记录
| 评审人 | 评审意见 | 状态 |
|--------|----------|------|
| {评审人1} | {意见} | ✅通过 / ⚠️需修改 |
| {评审人2} | {意见} | ✅通过 / ⚠️需修改 |

### C. 变更记录
| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | {日期} | 初始版本 | {作者} |

---

**评审状态**: ⬜ 待评审  ⬜ 评审通过  ⬜ 需修改  ⬜ 已批准
