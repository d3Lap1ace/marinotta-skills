# Git Auto Workspace

Resolve GitHub remotes into `~/Code/Github.com/<owner>/<repo>` and create or clone the local workspace before running git commands.

Example:

```bash
python3 scripts/git_auto_workspace.py --clone git@github.com:d3Lap1ace/gitso.git
```
